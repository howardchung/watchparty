import React from 'react';

// Placeholder component for handling lobby data later

const LobbyWrapper: React.FC = (props) => {
  const { children } = props;
  return <>{children}</>;
};

export default LobbyWrapper;

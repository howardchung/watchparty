import React from 'react';

// Placeholder component to hold the Playlist data later

const PlaylistWrapper: React.FC = (props) => {
  const { children } = props;

  return <>{children}</>;
};

export default PlaylistWrapper;
